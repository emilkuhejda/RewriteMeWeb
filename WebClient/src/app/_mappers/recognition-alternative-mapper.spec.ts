import { RecognitionAlternativeMapper } from './recognition-alternative-mapper';

describe('RecognitionAlternativeMapper', () => {
  it('should create an instance', () => {
    expect(new RecognitionAlternativeMapper()).toBeTruthy();
  });
});
