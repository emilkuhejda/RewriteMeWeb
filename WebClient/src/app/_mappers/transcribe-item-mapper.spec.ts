import { TranscribeItemMapper } from './transcribe-item-mapper';

describe('TranscribeItemMapper', () => {
  it('should create an instance', () => {
    expect(new TranscribeItemMapper()).toBeTruthy();
  });
});
