import { RecognitionStatePipe } from './recognition-state.pipe';

describe('RecognitionStatePipe', () => {
  it('create an instance', () => {
    const pipe = new RecognitionStatePipe();
    expect(pipe).toBeTruthy();
  });
});
