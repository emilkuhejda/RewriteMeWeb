import { StorageSettingPipe } from './storage-setting.pipe';

describe('StorageSettingPipe', () => {
  it('create an instance', () => {
    const pipe = new StorageSettingPipe();
    expect(pipe).toBeTruthy();
  });
});
