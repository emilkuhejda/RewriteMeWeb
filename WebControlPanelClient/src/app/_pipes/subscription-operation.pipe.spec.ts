import { SubscriptionOperationPipe } from './subscription-operation.pipe';

describe('SubscriptionOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new SubscriptionOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
