import { DeletedAccountMapper } from './deleted-account-mapper';

describe('DeletedAccountMapper', () => {
  it('should create an instance', () => {
    expect(new DeletedAccountMapper()).toBeTruthy();
  });
});
