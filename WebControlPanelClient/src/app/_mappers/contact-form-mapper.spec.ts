import { ContactFormMapper } from './contact-form-mapper';

describe('ContactFormMapper', () => {
  it('should create an instance', () => {
    expect(new ContactFormMapper()).toBeTruthy();
  });
});
