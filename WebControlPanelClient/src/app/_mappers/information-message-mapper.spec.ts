import { InformationMessageMapper } from './information-message-mapper';

describe('InformationMessageMapper', () => {
  it('should create an instance', () => {
    expect(new InformationMessageMapper()).toBeTruthy();
  });
});
