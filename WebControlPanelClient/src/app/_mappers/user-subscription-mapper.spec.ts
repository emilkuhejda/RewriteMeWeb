import { UserSubscriptionMapper } from './user-subscription-mapper';

describe('UserSubscriptionMapper', () => {
  it('should create an instance', () => {
    expect(new UserSubscriptionMapper()).toBeTruthy();
  });
});
