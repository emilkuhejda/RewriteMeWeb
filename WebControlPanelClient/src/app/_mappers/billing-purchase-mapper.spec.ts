import { BillingPurchaseMapper } from './billing-purchase-mapper';

describe('BillingPurchaseMapper', () => {
  it('should create an instance', () => {
    expect(new BillingPurchaseMapper()).toBeTruthy();
  });
});
