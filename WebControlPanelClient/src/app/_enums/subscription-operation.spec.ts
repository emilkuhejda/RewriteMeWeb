import { SubscriptionOperation } from './subscription-operation';

describe('SubscriptionOperation', () => {
  it('should create an instance', () => {
    expect(new SubscriptionOperation()).toBeTruthy();
  });
});
