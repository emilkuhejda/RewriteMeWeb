import { LanguageVersionMapper } from './language-version-mapper';

describe('LanguageVersionMapper', () => {
  it('should create an instance', () => {
    expect(new LanguageVersionMapper()).toBeTruthy();
  });
});
