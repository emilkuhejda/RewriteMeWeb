import { RecognitionWordInfoMapper } from './recognition-word-info-mapper';

describe('RecognitionWordInfoMapper', () => {
  it('should create an instance', () => {
    expect(new RecognitionWordInfoMapper()).toBeTruthy();
  });
});
