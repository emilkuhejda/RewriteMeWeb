import { TimeSpanWrapperMapper } from './time-span-wrapper-mapper';

describe('TimeSpanWrapperMapper', () => {
  it('should create an instance', () => {
    expect(new TimeSpanWrapperMapper()).toBeTruthy();
  });
});
