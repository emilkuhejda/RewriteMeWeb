import { RoundConfidencePipe } from './round-confidence.pipe';

describe('RoundConfidencePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundConfidencePipe();
    expect(pipe).toBeTruthy();
  });
});
