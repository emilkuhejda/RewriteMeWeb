import { RecognitionWordInfo } from './recognition-word-info';

describe('RecognitionWordInfo', () => {
  it('should create an instance', () => {
    expect(new RecognitionWordInfo()).toBeTruthy();
  });
});
