import { InformationMessage } from './information-message';

describe('InformationMessage', () => {
  it('should create an instance', () => {
    expect(new InformationMessage()).toBeTruthy();
  });
});
