import { TranscribeItemViewModel } from './transcribe-item-view-model';

describe('TranscribeItemViewModel', () => {
  it('should create an instance', () => {
    expect(new TranscribeItemViewModel()).toBeTruthy();
  });
});
